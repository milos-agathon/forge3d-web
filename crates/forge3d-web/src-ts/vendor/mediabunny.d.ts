// Type-only stand-in for the lock-controlled mediabunny@1.58.0 module.
// video.ts types the runtime exports it uses; the real module is loaded
// lazily and vendored into dist/vendor by prepare-dist.
export {};
